# Photoer 项目规范

本项目的所有实现都必须遵循 `docs/design-principles.md` 中的设计原则。做技术选型或实现细节时，优先选择行业标准、生产级别的主流做法，不为了实现速度牺牲规范性和安全性。

如果某个实现点在 `docs/` 现有文档（`decisions.md`、`auth-design.md`、`database-schema.md`、`design-principles.md`）中没有明确规定，需要自行做技术决策时，**必须在完成后明确告知用户做了哪个决策、为什么这么选、有什么代价**——不能默默处理、不留痕迹。
